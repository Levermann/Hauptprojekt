package com.levermann.kennzahlen;

import java.util.Scanner;
/*
import org.hibernate.Query;
import org.hibernate.Session;
import org.hibernate.SessionFactory;
import org.hibernate.Transaction;
import org.hibernate.cfg.Configuration;

public class Kursverlauf6Monate {

    public void Kursverlauf6Monate(Kursverlauf6Monate p) {

        //Diese Klasse benutzt die Aktuelle Session der Klasse HibernateUtil
        Session session = HibernateUtil.getSessionFactory().getCurrentSession();
        Transaction transaction = session.beginTransaction();
        session.saveOrUpdate(p);
        transaction.commit();


        Scanner scanner = new Scanner(System.in);
        float KursVor6Monaten = scanner.nextInt();
        float KursHeute = scanner.nextInt();




    }


}

        */